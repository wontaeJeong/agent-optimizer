{
  "$schema": "https://opencode.ai/config.json",
  "model": "compatible/{env:AGENT_OPT_MODEL_ID}",
  "small_model": "compatible/{env:AGENT_OPT_MODEL_ID}",
  "enabled_providers": ["compatible"],
  "provider": {
    "compatible": {
      "npm": "@ai-sdk/openai-compatible",
      "name": "OpenAI-compatible endpoint",
      "options": {
        "baseURL": "{env:AGENT_OPT_MODEL_BASE_URL}",
        "apiKey": "{env:AGENT_OPT_MODEL_API_KEY}"
      },
      "models": {"{env:AGENT_OPT_MODEL_ID}": {"name": "Configured model"}}
    }
  },
  "autoupdate": false,
  "share": "disabled"
}
