from agent_optimizer.contracts import UnavailableError

class Evaluator:
    def __init__(self, config=None):
        pass

    def evaluate(self, task, output_dir, timeout_seconds):
        raise UnavailableError("Connect this team's real tests and metrics; never trust agent-reported success")
