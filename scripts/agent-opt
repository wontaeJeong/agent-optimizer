#!/usr/bin/env python3
"""User CLI bootstrap: protect doctor imports before loading the package."""
import sys

if len(sys.argv) > 1 and sys.argv[1] == "doctor":
    sys.dont_write_bytecode = True

from agent_optimizer.cli import main

raise SystemExit(main())
